{
  "name": "whatsapp-chat-analyzer",
  "version": "1.0.0",
  "description": "AI-powered WhatsApp chat analyzer chatbot",
  "private": true,
  "scripts": {
    "dev": "echo \"To run the app, open two terminals. In Terminal 1: npm run dev:server | In Terminal 2: npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
